# ADR-0001 — Arithmetic API

Status: Accepted

`src/math.ts` is the authoritative fixture module for the `add` function.
Changes to its public arithmetic API require the related test in `tests/math.test.ts`.
